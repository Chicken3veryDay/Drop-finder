# Shared COA association handoff

Agent: `38c869b3-68e2-4dd9-9d3b-6e479e282614`  
Run: `d539df25-0079-417b-ad75-77939f0fbb93`  
Cycle: `953b129b-e22c-409f-ad5a-ffb771fcdc4b`  
Claim: `5a4d21ec-fc7e-4844-a29a-13d0129b0f51`  
Repository: `Chicken3veryDay/Drop-finder`  
Work mode: `TEST_HARDENING`  
Task: issue #49, preserve distinct structured-JSON COA association edges sharing one canonical URL  
Status: `BLOCKED` / claim released after this checkpoint  
Base SHA: `b73db66d217e099195ee00c898079cbae18c8ab4`  
Branch: `agent/38c869b3/953b129b/shared-coa-associations`  
Current application SHA before this handoff: `7b309bf11ca66a1675189083f0902b1e0ea145e2`  
Last known-good SHA: `7b309bf11ca66a1675189083f0902b1e0ea145e2`  
UTC updated: `2026-07-16T21:31:45Z`

## Problem and root cause

`discover_json_documents()` deduplicated candidates by canonical URL before constructing `DocumentCandidate`. The URL identifies a physical resource, but it does not identify a product, variant, or batch association. Later explicit edges were lost even though the existing stable document identity includes those association fields.

## Changes

- `tests/test_vendor_document_associations.py` adds regressions for two distinct associations sharing one URL, exact downstream mapping, true duplicates, and tracking-parameter canonicalization.
- `scripts/vendor_adapters/discovery.py` constructs candidates before deduplication and uses `candidate.document_id` as the structured-JSON dedupe key.

Commits:

- `a37533aa54ba48c67171b2d497934b688bda6122` test reproduction
- `7b309bf11ca66a1675189083f0902b1e0ea145e2` root-cause correction

## Validation

A deterministic source-equivalent reconstruction of current URL canonicalization, candidate identity, discovery, and mapping produced:

```text
before: distinct shared-URL associations=1, true duplicates=1
after:  distinct shared-URL associations=2, true duplicates=1
mapping: each product's exact candidate scored 355 versus 50 for the unrelated shared-URL candidate
```

No full repository command was represented as run because the execution environment cannot clone the repository. The final diff before this handoff contained only the intended source change and focused test file. Current `main` advanced by one generated-catalog commit that does not touch the affected paths.

## Blocker

GitHub temporarily rejected new issue comments and pull-request creation under a secondary content-creation rate limit. Repository reads, branch commits, issue assignment updates, and existing-comment updates remained available.

## Safety

The branch is isolated and recoverable. No default-branch files or issue contents were changed. The correction is small, deterministic, and has no dependency, schema, or migration impact. Exact-head CI has not run because a pull request could not be opened.

## Exact next command/action

Create a draft pull request from `agent/38c869b3/953b129b/shared-coa-associations` to `main`, link issue #49, and run the repository's Python test suite. The PR description should include the root cause, two commits above, source-equivalent validation, one-commit generated-catalog base drift, risk, and rollback. If current `main` changes either affected path first, rebase or recreate the two focused commits and rerun validation.
