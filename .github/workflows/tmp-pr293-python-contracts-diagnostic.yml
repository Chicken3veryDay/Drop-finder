name: Temporary PR 293 Python Contract Diagnostic

on:
  push:
    branches:
      - verification/pr293-python-contracts
  pull_request:
    branches:
      - verification/pr293-python-contracts

permissions:
  contents: read

jobs:
  diagnose:
    if: github.head_ref == 'trigger/pr293-python-contracts'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: verification/pr293-python-contracts
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run exact multi-product contract command
        shell: bash
        run: |
          set +e
          python -m compileall -q scripts/multi_product tests/multi_product
          python -m unittest discover -s tests/multi_product -v > /tmp/pr293-python-contracts.log 2>&1
          status=$?
          printf '%s\n' "$status" > /tmp/pr293-python-contracts.status
          tail -n 320 /tmp/pr293-python-contracts.log
          exit 0
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pr293-python-contracts-diagnostic
          path: |
            /tmp/pr293-python-contracts.log
            /tmp/pr293-python-contracts.status
      - name: Preserve test result
        run: test "$(cat /tmp/pr293-python-contracts.status)" = "0"
