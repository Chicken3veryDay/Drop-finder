name: Temporary PR 356 Merge Diagnostic

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  diagnose:
    if: github.head_ref == 'fix/issue-90-bounded-detail-shards'
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Capture merge-ref Catalog V4 validation
        shell: bash
        run: |
          set +e
          {
            python -m compileall -q scripts/catalog_v4 tests/catalog_v4
            python -m unittest discover -s tests/catalog_v4 -v
            python -m scripts.catalog_v4.selftest
          } > /tmp/pr356-merge.log 2>&1
          printf '%s\n' "$?" > /tmp/pr356-merge.status
          tail -400 /tmp/pr356-merge.log
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: pr356-merge
          path: |
            /tmp/pr356-merge.log
            /tmp/pr356-merge.status
          if-no-files-found: error
          retention-days: 7
