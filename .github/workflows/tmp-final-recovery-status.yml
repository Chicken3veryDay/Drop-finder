name: Temporary Final Recovery Status

on:
  pull_request:
    branches: [verification/balanced-recovery-publish]

permissions:
  actions: read
  contents: read

jobs:
  snapshot:
    if: github.head_ref == 'trigger/balanced-recovery-publish'
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Snapshot exact source workflows
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          TARGET_SHA: e4f9265c6fc74ea26ea132a8c1caa25b112ef73d
        shell: bash
        run: |
          set -euo pipefail
          gh api "repos/${REPOSITORY}/actions/runs?head_sha=${TARGET_SHA}&event=push&per_page=100" > runs.json
          python - <<'PY'
          import json, os, subprocess
          data = json.load(open('runs.json', encoding='utf-8'))
          wanted = {'DropFinder Cloud CI', 'DropFinder Autonomous Cloud'}
          latest = {}
          for run in data.get('workflow_runs', []):
              name = run.get('name')
              if name in wanted and name not in latest:
                  jobs = json.loads(subprocess.check_output([
                      'gh', 'api', f"repos/{os.environ['REPOSITORY']}/actions/runs/{run['id']}/jobs?per_page=100"
                  ]))
                  latest[name] = {
                      'id': run.get('id'),
                      'status': run.get('status'),
                      'conclusion': run.get('conclusion'),
                      'created_at': run.get('created_at'),
                      'updated_at': run.get('updated_at'),
                      'jobs': [
                          {
                              'id': job.get('id'),
                              'name': job.get('name'),
                              'status': job.get('status'),
                              'conclusion': job.get('conclusion'),
                              'started_at': job.get('started_at'),
                              'completed_at': job.get('completed_at'),
                          }
                          for job in jobs.get('jobs', [])
                      ],
                  }
          json.dump(latest, open('final-recovery-status.json', 'w', encoding='utf-8'), indent=2, sort_keys=True)
          print(json.dumps(latest, indent=2, sort_keys=True))
          PY
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: final-recovery-status
          path: final-recovery-status.json
          if-no-files-found: error
          retention-days: 1
