{
  "name": "dropfinder-platform-workstream",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "typecheck": "node scripts/typecheck.mjs",
    "benchmark": "node scripts/performance/benchmark.mjs",
    "validate": "npm run typecheck && npm test && npm run benchmark",
    "test:e2e": "node tests/e2e/run-playwright.mjs"
  }
}
